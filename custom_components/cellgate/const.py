"""Constants for the Cellgate integration."""

from datetime import timedelta

DOMAIN = "cellgate"
BASE_URL = "https://cellgate.zapopen.com"
API_PREFIX = "api/CellCam"
DEFAULT_SCAN_INTERVAL = timedelta(hours=6)
APP_VERSION = "17"
APP_TYPE = "android"
